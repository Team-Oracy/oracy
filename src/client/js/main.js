jQuery(document).ready(function($) {

  // Variables

  // Listeners
  $('#retryLoadingAudiobooks').on('click', retryLoadingAudiobooks)

  // Init

  // Functions
  function retryLoadingAudiobooks() {
    location.reload()
  }  

})
